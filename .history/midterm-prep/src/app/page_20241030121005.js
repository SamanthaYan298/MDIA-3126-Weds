import Image from "next/image";

export default function Home() {
  // Implementation
  // - Pick an API
  // - Build a button component that has a fetch action
  // - Build a component that display our data (should have an empty and fulfilled)

  return (
    <div className="m-8">
      This should work if everything is going ok.
    </div>
  );
}
