import Image from "next/image";

export default function Home() {
  // Implementation
  // x Pick an API
  // - Build a button component that has a fetch action/clear action
  // - Build a component that display our data (should have an empty and fulfilled state)
  // - Build a function that will fetch some data
  // - Format and  handle the data
  // - (error handling)
  // - Style our app and create breakpoints
  // - Component for out button to sit in

  return (
    <div className="m-8">
      <section>
        <h1>My cool midterm project</h1>
        <button>Fetch </button>
      </section>
      <section>Here is where I will display my data</section>
    </div>
  );
}
